# Documentation sync: 2026-09-05

Direction: code-to-docs, inferred from the completed implementation. Scope: current working-tree
changes affecting README, configuration, app usage, architecture and output contracts.
Historical reviews, governance documents, generated diagrams and the full repository were not audited.

## Corrections applied

- README: CPU support and commands preserving the explicitly installed Paddle runtime.
- Configuration: active Luna pricing, runtime model defaults, configured selective routing versus
  fail-closed acceptance, CLI/UI worker differences and launcher runtime behavior.
- App usage: confidence JSON download, v3 artifact description and runtime-preserving startup.
- Architecture: bounded independent reads, 1,600-pixel layout inputs and configurable evaluation workers.
- Output contract: distinguish nullable field confidence from failed-page/document zero values.

Evidence: `pyproject.toml`, `scripts/launch.ps1`, `src/ade_app/config.py`, `constants.py`,
`batch.py`, `cli.py`, `pipeline.py`, `outputs.py`, `openai_client.py`, `layout.py`,
`evaluation.py`, `services/confidence.py`, and `streamlit_app.py`.
The graph narrowed the changed-code scope; source inspection determined corrections.

Changed documentation: `README.md`, `docs/reference/configuration.md`,
`docs/how-to/use-the-app.md`, `docs/explanation/architecture.md`,
`docs/reference/output-contract.md`, and this report.

## Validation

- `uv run --no-sync pytest tests/test_documentation.py`: checks local Markdown links and Python example syntax.
- `git diff --check -- README.md docs/reference/configuration.md docs/how-to/use-the-app.md docs/explanation/architecture.md docs/reference/output-contract.md`: whitespace validation.
- No application behavior changed; no paid requests or native OCR runs required.

## Remaining items

No human-review conflicts in the audited scope. The launcher issue identified during this sync
was subsequently fixed: it now invokes `uv run --no-sync` to preserve the installed Paddle extra.

`aiwg artifacts path` failed because the AIWG CLI is unavailable. No `AIWG_ARTIFACTS_PATH`
or `.aiwg-location` redirect was present. This report uses an explicit documentation fallback;
no AIWG artifact root or last-run state was created. AIWG sync-state recording remains unavailable.
